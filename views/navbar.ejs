<nav>
  <ul>
    <% for (let i = 0; i < links.length; i++) { %>
      <a href="<%= links[i].href %>">
        <button type="button">
          <span> <%= links[i].text %> </span>
        </button>
    </a>
    <% } %>
  </ul>
</nav>